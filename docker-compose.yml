services:
  jsrw:
    image: nginx:1.27-alpine
    container_name: jsrw-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:18082:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
